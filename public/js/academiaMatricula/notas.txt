EDITAR
1.Al editar un grupo, hay muchos problemas si se permite seleccionar una actividad individual, ¿que pasa con los demas atletas?
2.Igual que la anterior pero alrevez, si un atleta esta en una actividad individual y edita para hacer un grupo.
3.Si se edita no se esta tomando en cuenta la restriccion de atleta repetido.
4.Corregir la validacion de sexos en submodalidades
5.Categorias no se selecciona automaticamente al editar



UPDATE modalidades SET nombre = 'Combate' WHERE id_modalidad = 1;

UPDATE submodalidades
SET cantidad_atletas = CASE id_subModalidad
    WHEN 1 THEN 1
    WHEN 2 THEN 3
    WHEN 3 THEN 1
    WHEN 4 THEN 2
    WHEN 5 THEN 5
    ELSE cantidad_atletas
END
WHERE id_subModalidad IN (1, 2, 3, 4, 5);